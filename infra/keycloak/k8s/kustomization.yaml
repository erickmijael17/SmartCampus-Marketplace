apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: ecom

generatorOptions:
  disableNameSuffixHash: true

configMapGenerator:
  - name: keycloak-realm-import
    namespace: ecom
    files:
      - realm-import.json

resources:
  - keycloak-db.yaml
  - keycloak.yaml
