name: ecom-carrito-prod

services:
  postgres-carrito:
    image: postgres:16-alpine
    container_name: ecom-postgres-carrito
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${DB_NAME}
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASS}
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER} -d ${DB_NAME}"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 20s
    volumes:
      - ecom_carrito_data:/var/lib/postgresql/data
    ports:
      - "25434:5432"
    networks:
      - ecom-carrito-int

  carrito-ms:
    build: .
    container_name: ecom-carrito-ms
    restart: unless-stopped
    depends_on:
      postgres-carrito:
        condition: service_healthy
    environment:
      SPRING_PROFILES_ACTIVE: ${SPRING_PROFILES_ACTIVE}
      CONFIG_SERVER_URL: ${CONFIG_SERVER_URL}
      JWT_SECRET: ${JWT_SECRET}
      DB_HOST: ecom-postgres-carrito
      DB_PORT: 5432
      DB_NAME: ${DB_NAME}
      DB_USER: ${DB_USER}
      DB_PASS: ${DB_PASS}
    ports:
      - "8084:8080"
    volumes:
      - ./logs:/app/logs
    networks:
      - ecom-carrito-int
      - ecom-prod-net

volumes:
  ecom_carrito_data:

networks:
  ecom-prod-net:
    external: true
    name: ecom-prod-net
  ecom-carrito-int:
    name: ecom-carrito-int
