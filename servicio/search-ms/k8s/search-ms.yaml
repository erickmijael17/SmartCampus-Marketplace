# =============================================================================
# SEARCH-MS: Secret + PostgreSQL + Microservicio + HPA
# Motor de búsqueda del marketplace usando PostgreSQL full-text search
# (Se puede migrar a Elasticsearch en el futuro sin cambiar la estructura K8s)
# =============================================================================
apiVersion: v1
kind: Secret
metadata:
  name: search-ms-secret
  namespace: ecom
  labels:
    app: search-ms
type: Opaque
stringData:
  db-name: "search_db"
  db-user: "search_user"
  db-pass: "search_db_secure_2024"
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: search-ms-postgres-pvc
  namespace: ecom
  labels:
    app: postgres-search
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres-search
  namespace: ecom
  labels:
    app: postgres-search
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres-search
  template:
    metadata:
      labels:
        app: postgres-search
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              valueFrom:
                secretKeyRef:
                  name: search-ms-secret
                  key: db-name
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: search-ms-secret
                  key: db-user
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: search-ms-secret
                  key: db-pass
          volumeMounts:
            - name: postgres-data
              mountPath: /var/lib/postgresql/data
          livenessProbe:
            exec:
              command: ["pg_isready", "-U", "search_user"]
            initialDelaySeconds: 30
            periodSeconds: 10
          resources:
            requests:
              memory: "512Mi"
              cpu: "200m"
            limits:
              memory: "1Gi"
              cpu: "1000m"
      volumes:
        - name: postgres-data
          persistentVolumeClaim:
            claimName: search-ms-postgres-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: postgres-search
  namespace: ecom
spec:
  type: ClusterIP
  selector:
    app: postgres-search
  ports:
    - port: 5432
      targetPort: 5432
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: search-ms
  namespace: ecom
  labels:
    app: search-ms
    app.kubernetes.io/component: microservice
spec:
  replicas: 1
  selector:
    matchLabels:
      app: search-ms
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels:
        app: search-ms
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/path: "/actuator/prometheus"
        prometheus.io/port: "8080"
    spec:
      initContainers:
        - name: wait-for-config-server
          image: curlimages/curl:8.5.0
          command: ["sh", "-c", "until curl -sf http://config-server:8888/actuator/health; do sleep 5; done"]
        - name: wait-for-eureka
          image: curlimages/curl:8.5.0
          command: ["sh", "-c", "until curl -sf http://eureka:8761/actuator/health; do sleep 5; done"]
        - name: wait-for-db
          image: postgres:16-alpine
          command: ["sh", "-c", "until pg_isready -h postgres-search -p 5432 -U search_user; do sleep 3; done"]
      containers:
        - name: search-ms
          image: erickmijael17/ecom-search-ms:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "prod"
            - name: CONFIG_SERVER_URL
              value: "http://config-server:8888"
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: ecom-global-secret
                  key: jwt-secret
            - name: DB_HOST
              value: "postgres-search"
            - name: DB_PORT
              value: "5432"
            - name: DB_NAME
              valueFrom:
                secretKeyRef:
                  name: search-ms-secret
                  key: db-name
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: search-ms-secret
                  key: db-user
            - name: DB_PASS
              valueFrom:
                secretKeyRef:
                  name: search-ms-secret
                  key: db-pass
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            initialDelaySeconds: 120
            periodSeconds: 15
            failureThreshold: 5
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8080
            initialDelaySeconds: 90
            periodSeconds: 10
            failureThreshold: 10
          resources:
            requests:
              memory: "512Mi"
              cpu: "300m"
            limits:
              memory: "1.5Gi"
              cpu: "1500m"
---
apiVersion: v1
kind: Service
metadata:
  name: search-ms
  namespace: ecom
  labels:
    app: search-ms
spec:
  type: ClusterIP
  selector:
    app: search-ms
  ports:
    - name: http
      port: 8080
      targetPort: 8080
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: search-ms-hpa
  namespace: ecom
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: search-ms
  minReplicas: 1
  maxReplicas: 5
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 60
